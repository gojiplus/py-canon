"""{{ description }}{% if not description.endswith(".") %}.{% endif %}"""

from importlib.metadata import PackageNotFoundError, version

try:
    __version__ = version("{{ project_name }}")
except PackageNotFoundError:  # pragma: no cover - not installed
    __version__ = "0.0.0"
