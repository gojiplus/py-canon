name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

jobs:
  release:
    uses: ./.github/workflows/reusable-release.yml
    permissions:
      contents: write
      id-token: write
